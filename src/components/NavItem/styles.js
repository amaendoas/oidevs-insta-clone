import styled from "styled-components";

export const Container = styled.button`
  cursor: pointer;
  width: 100%;
  display: flex;
  align-items: center;
  gap: 1rem;

  :hover {
    filter: brightness(0.6);
  }
`